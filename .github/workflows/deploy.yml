# GitHub Actions Deployment (OPTIONAL - NOT REQUIRED)
#
# This workflow is DISABLED by default.
# Vercel's native GitHub integration is simpler - just connect your repo in Vercel dashboard.
#
# To enable this workflow:
# 1. Remove the "# " from "on:" line below
# 2. Set up GitHub secrets (see DEPLOYMENT.md)
#
# name: Deploy to Vercel
# 
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
# 
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#       
#       - name: Deploy to Vercel
#         uses: amondnet/vercel-action@v25
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#           vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#           working-directory: ./
#           vercel-args: '--prod'

